# Dependencies (regenerate with: npm install)
node_modules/

# Bundled runtimes (Node + Python distributions, ~150 MB)
runtime/

# Local TTS voice models (~1.2 GB of .onnx files; download separately)
models/

# Compiled launcher binary (rebuild from source if needed)
*.exe
*.dll

# Secrets and local environment
.env
.env.*
!.env.example

# Local configuration with credentials / machine-specific settings
config.json
config.local.json

# Logs and build output
*.log
dist/
build/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~
